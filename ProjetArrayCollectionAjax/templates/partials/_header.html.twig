<style>
.navbar {
  background-color: #f2ead0 ;
  border-bottom: none;
}

</style>


<nav class="navbar navbar-expand-lg navbar-light ">
  <div class="container-fluid">
    <a class="navbar-brand" href="{{path('app_home')}}" style="color:green; font-size:20px">MealMatch</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor03" aria-controls="navbarColor03" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
  
      <form class="d-flex">
      <ul class="navbar-nav me-auto">
					{% if app.user %}
          <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" data-bs-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">{{app.user.prenom}} {{app.user.nom}}</a>
          <div class="dropdown-menu">
            <a class="dropdown-item" href="{{path('recette_index')}}">Mes recettes</a>
            <a class="dropdown-item" href="{{path('recette_creation')}}">Ajouter une recette</a>
            <a class="dropdown-item" href="{{path('app_logout')}}">Déconnexion</a>
            
          </div>
        </li>
        {% else %}
						<li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle " data-bs-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false"> <i class="bi bi-list"></i>
 </a>
          <div class="dropdown-menu ms-1">
            <a class="dropdown-item" href="{{path('app_login')}}">Connexion</a>
            <a class="dropdown-item" href="{{path('app_register')}}">Inscription</a>
            <a class="dropdown-item" href="#"></a>
            
          </div>
        </li>
					{% endif %}
        </ul>

        </div>


        
      </form>
    </div>
  </div>
</nav>
